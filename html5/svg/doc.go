// Code generated by fluent generator. DO NOT EDIT.

// Package svg builds SVG documents as a typed vocabulary. The root element is
// created with New, and it covers the drawable shapes, the structural and
// accessibility elements, text, paint servers, filters and animation.
//
// Every element takes the children SVG allows it, through the constructor and
// the Add and Replace methods used everywhere else in fluent:
//
//	svg.Circle(
//		svg.Title("Q3 revenue"),
//		svg.Animate().AttributeName("r").To("30"),
//	).Cx("50").Cy("50").R("20")
//
// Which children an element takes is sealed by interface, and the interface a
// constructor names says which. Shape is what a container accepts. ShapeContent
// is what a shape, Use or Image accepts: descriptive elements, animation
// elements, paint servers, ClipPath, Marker, Mask, Script and Style.
// PrimitiveContent is what a filter primitive, light source, transfer function
// or Stop accepts: the descriptive elements, Animate, Set, Script and Style.
// Descriptive is what an animation element, MPath or View accepts.
// FilterPrimitive, LightSource and TransferFunction seal the children of
// Filter, the lighting primitives and FeComponentTransfer.
//
// So nesting a shape in a shape, a primitive outside a Filter, or any non-SVG
// node, is a compile error. The interfaces group elements as the specification
// does; they do not encode every contextual restriction in it. Some elements
// take only their own typed children, which is narrower than the specification:
// Filter takes primitives, the gradients take Stop, FeMerge takes FeMergeNode,
// FeComponentTransfer takes transfer functions, the lighting primitives take a
// LightSource, and AnimateMotion takes MPath. In the other direction a
// container that accepts Shape accepts any of them, which is wider than any one
// container's model. Both are deliberate: the aim is ordinary composition that
// reads well and catches the mistakes worth catching.
//
// Use Raw to embed a verbatim SVG fragment, and for content that is not markup:
// the Style and Script elements take their stylesheet or script through Raw,
// because CSS and JavaScript must not be HTML-escaped.
package svg
