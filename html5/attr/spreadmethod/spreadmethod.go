// Code generated by fluent generator. DO NOT EDIT.

// Package spreadmethod defines the [SpreadMethod] type and its predefined values.
//
// Controls how a gradient paints outside its defined range. Pad extends the
// end colours, reflect alternates forward and reversed copies, and repeat
// restarts the gradient in the same direction. It applies to both linear and
// radial gradients. Pad is the default.
package spreadmethod

import (
	"github.com/jpl-au/fluent/node"
)

// SpreadMethod is a typed value for the SVG spreadMethod attribute.
//
// Controls how a gradient paints outside its defined range. Pad extends the
// end colours, reflect alternates forward and reversed copies, and repeat
// restarts the gradient in the same direction. It applies to both linear and
// radial gradients. Pad is the default.
type SpreadMethod []byte

// Variables for SpreadMethod values
var (
	// Pad The end colours extend to fill the rest of the shape. This is the SVG default.
	Pad = SpreadMethod("pad")

	// Reflect The gradient repeats in reverse, then forward again, so adjacent copies mirror each other with no hard edge.
	Reflect = SpreadMethod("reflect")

	// Repeat The gradient repeats from the start, so each copy begins with a hard edge back to the first colour.
	Repeat = SpreadMethod("repeat")
)

// Custom allows setting a custom SpreadMethod value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) SpreadMethod {
	return SpreadMethod(node.EscapeAttribute(value))
}
