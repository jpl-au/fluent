// Code generated by fluent generator. DO NOT EDIT.

// Package cliprule defines the [ClipRule] type and its predefined values.
//
// Determines which regions of shapes inside a clipPath belong to the clipping
// area. Nonzero uses the direction and winding of the paths. Evenodd alternates
// inside and outside at each crossing. This attribute affects clipping geometry,
// not the fill of shapes outside the clipPath.
package cliprule

import (
	"github.com/jpl-au/fluent/node"
)

// ClipRule is a typed value for the SVG clip-rule attribute.
//
// Determines which regions of shapes inside a clipPath belong to the clipping
// area. Nonzero uses the direction and winding of the paths. Evenodd alternates
// inside and outside at each crossing. This attribute affects clipping geometry,
// not the fill of shapes outside the clipPath.
type ClipRule []byte

// Variables for ClipRule values
var (
	// NonZero A point is inside the clip when the shape winds around it a non-zero number of times. This is the SVG default.
	NonZero = ClipRule("nonzero")

	// EvenOdd A point is inside the clip when a ray from it crosses the shape an odd number of times, so nested shapes cut holes in the clip region.
	EvenOdd = ClipRule("evenodd")
)

// Custom allows setting a custom ClipRule value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) ClipRule {
	return ClipRule(node.EscapeAttribute(value))
}
