// Code generated by fluent generator. DO NOT EDIT.

// Package edgemode defines the [EdgeMode] type and its predefined values.
//
// Controls how feGaussianBlur and feConvolveMatrix sample beyond the edge of
// an input image. Duplicate extends the edge pixels, wrap samples from the
// opposite edge, and none supplies transparent black. The choice affects the
// filter result near the image boundary.
package edgemode

import (
	"github.com/jpl-au/fluent/node"
)

// EdgeMode is a typed value for the SVG edgeMode attribute.
//
// Controls how feGaussianBlur and feConvolveMatrix sample beyond the edge of
// an input image. Duplicate extends the edge pixels, wrap samples from the
// opposite edge, and none supplies transparent black. The choice affects the
// filter result near the image boundary.
type EdgeMode []byte

// Variables for EdgeMode values
var (
	// Duplicate The edge pixels are repeated outwards, which avoids a visible border but smears the edge colour.
	Duplicate = EdgeMode("duplicate")

	// Wrap The input is tiled, so pixels are taken from the opposite edge. It suits a texture that already tiles.
	Wrap = EdgeMode("wrap")

	// None Pixels beyond the edge are transparent black, which lets a blur fade out at the boundary. This is the default for feGaussianBlur.
	None = EdgeMode("none")
)

// Custom allows setting a custom EdgeMode value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) EdgeMode {
	return EdgeMode(node.EscapeAttribute(value))
}
