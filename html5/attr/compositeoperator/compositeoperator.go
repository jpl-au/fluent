// Code generated by fluent generator. DO NOT EDIT.

// Package compositeoperator defines the [CompositeOperator] type and its predefined values.
//
// Selects how feComposite combines its source input, in, with its destination
// input, in2. Porter-Duff operations combine the inputs according to their alpha
// coverage. Arithmetic uses the k1, k2, k3 and k4 coefficients to calculate each
// output channel.
package compositeoperator

import (
	"github.com/jpl-au/fluent/node"
)

// CompositeOperator is a typed value for the SVG feComposite operator attribute.
//
// Selects how feComposite combines its source input, in, with its destination
// input, in2. Porter-Duff operations combine the inputs according to their alpha
// coverage. Arithmetic uses the k1, k2, k3 and k4 coefficients to calculate each
// output channel.
type CompositeOperator []byte

// Variables for CompositeOperator values
var (
	// Over The first input is drawn over the second. This is the default.
	Over = CompositeOperator("over")

	// In Only the parts of the first input that overlap the second are kept, which clips it to the second's shape.
	In = CompositeOperator("in")

	// Out Only the parts of the first input outside the second are kept, which cuts the second's shape out of it.
	Out = CompositeOperator("out")

	// Atop The first input is drawn over the second but clipped to it, so the result has the second's outline.
	Atop = CompositeOperator("atop")

	// Xor The parts where exactly one input is present are kept, and the overlap is removed.
	Xor = CompositeOperator("xor")

	// Lighter The two inputs are added together, which brightens where they overlap.
	Lighter = CompositeOperator("lighter")

	// Arithmetic Compute k1·i1·i2 + k2·i1 + k3·i2 + k4 for each channel, using the k1 to k4 attributes, and clamp
	// the result to the range 0 to 1.
	Arithmetic = CompositeOperator("arithmetic")
)

// Custom allows setting a custom CompositeOperator value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) CompositeOperator {
	return CompositeOperator(node.EscapeAttribute(value))
}
