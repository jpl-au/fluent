// Code generated by fluent generator. DO NOT EDIT.

// Package restart defines the [Restart] type and its predefined values.
//
// Controls whether a new begin condition can restart an SVG animation.
// Always permits restarting while it is active. WhenNotActive waits until it
// has finished. Never prevents restarting after the first activation. Repetition
// within an activation is controlled separately by repeatCount and repeatDur.
package restart

import (
	"github.com/jpl-au/fluent/node"
)

// Restart is a typed value for the SVG restart attribute.
//
// Controls whether a new begin condition can restart an SVG animation.
// Always permits restarting while it is active. WhenNotActive waits until it
// has finished. Never prevents restarting after the first activation. Repetition
// within an activation is controlled separately by repeatCount and repeatDur.
type Restart []byte

// Variables for Restart values
var (
	// Always The animation can restart at any time. This is the default.
	Always = Restart("always")

	// WhenNotActive The animation can restart only once it has finished, so a repeated trigger does not cut it short.
	WhenNotActive = Restart("whenNotActive")

	// Never The animation runs once and no later trigger restarts it.
	Never = Restart("never")
)

// Custom allows setting a custom Restart value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) Restart {
	return Restart(node.EscapeAttribute(value))
}
