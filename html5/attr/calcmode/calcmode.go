// Code generated by fluent generator. DO NOT EDIT.

// Package calcmode defines the [CalcMode] type and its predefined values.
//
// Selects how an SVG animation moves between the values it specifies. Discrete
// changes in steps, linear interpolates each interval, paced maintains an even
// rate, and spline uses the curves in keySplines. The default is linear for
// animate and animateTransform, and paced for animateMotion.
package calcmode

import (
	"github.com/jpl-au/fluent/node"
)

// CalcMode is a typed value for the SVG calcMode attribute.
//
// Selects how an SVG animation moves between the values it specifies. Discrete
// changes in steps, linear interpolates each interval, paced maintains an even
// rate, and spline uses the curves in keySplines. The default is linear for
// animate and animateTransform, and paced for animateMotion.
type CalcMode []byte

// Variables for CalcMode values
var (
	// Linear Each interval is interpolated at a constant rate. This is the default for most animation elements.
	Linear = CalcMode("linear")

	// Discrete The value jumps from one to the next with no interpolation.
	Discrete = CalcMode("discrete")

	// Paced The rate is constant across the whole animation rather than per interval, which keeps motion along a path even. This is the default for animateMotion.
	Paced = CalcMode("paced")

	// Spline Each interval follows a Bezier curve given by keySplines, which is how easing is expressed.
	Spline = CalcMode("spline")
)

// Custom allows setting a custom CalcMode value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) CalcMode {
	return CalcMode(node.EscapeAttribute(value))
}
