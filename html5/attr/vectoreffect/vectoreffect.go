// Code generated by fluent generator. DO NOT EDIT.

// Package vectoreffect defines the [VectorEffect] type and its predefined values.
//
// How the stroke of an element responds to transforms on the element and its
// ancestors. A typed value so only valid vector-effect keywords compile.
package vectoreffect

import (
	"github.com/jpl-au/fluent/node"
)

// VectorEffect is a typed value for the SVG vector-effect attribute.
//
// How the stroke of an element responds to transforms on the element and its
// ancestors. A typed value so only valid vector-effect keywords compile.
type VectorEffect []byte

// Variables for VectorEffect values
var (
	// None The stroke scales, rotates and skews with the element. This is the SVG default.
	None = VectorEffect("none")

	// NonScalingStroke The stroke width stays constant in screen pixels when the element or the viewport scales. Charts and diagrams usually want this on axis and grid lines.
	NonScalingStroke = VectorEffect("non-scaling-stroke")

	// NonScalingSize The element keeps its size when its ancestors scale. Position still follows the transforms.
	// Experimental: SVG 2 marks this value at risk due to missing implementations.
	// Do not rely on this effect without checking the target browsers.
	NonScalingSize = VectorEffect("non-scaling-size")

	// NonRotation The element keeps its orientation when its ancestors rotate. Position and scale still follow the transforms.
	// Experimental: SVG 2 marks this value at risk due to missing implementations.
	// Do not rely on this effect without checking the target browsers.
	NonRotation = VectorEffect("non-rotation")

	// FixedPosition The element keeps its position when its ancestors are translated. Rotation and scale still follow the transforms.
	// Experimental: SVG 2 marks this value at risk due to missing implementations.
	// Do not rely on this effect without checking the target browsers.
	FixedPosition = VectorEffect("fixed-position")
)

// Custom allows setting a custom VectorEffect value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) VectorEffect {
	return VectorEffect(node.EscapeAttribute(value))
}
