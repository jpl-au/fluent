// Code generated by fluent generator. DO NOT EDIT.

// Package units defines the [Units] type and its predefined values.
//
// Selects the coordinate system used by gradients, clip paths, masks, patterns
// and filters. UserSpaceOnUse uses the current user coordinate system.
// ObjectBoundingBox uses coordinates relative to the referenced element's
// bounding box. Geometry and child content can use separate unit attributes,
// and the default depends on the attribute.
package units

import (
	"github.com/jpl-au/fluent/node"
)

// Units is a typed value for the SVG coordinate-system attributes.
//
// Selects the coordinate system used by gradients, clip paths, masks, patterns
// and filters. UserSpaceOnUse uses the current user coordinate system.
// ObjectBoundingBox uses coordinates relative to the referenced element's
// bounding box. Geometry and child content can use separate unit attributes,
// and the default depends on the attribute.
type Units []byte

// Variables for Units values
var (
	// UserSpaceOnUse Coordinates are in the user coordinate system in place when the element is referenced, so the values are absolute lengths.
	UserSpaceOnUse = Units("userSpaceOnUse")

	// ObjectBoundingBox Coordinates are fractions of the bounding box of the element that references this one, so 0.5 is its centre.
	ObjectBoundingBox = Units("objectBoundingBox")
)

// Custom allows setting a custom Units value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) Units {
	return Units(node.EscapeAttribute(value))
}
