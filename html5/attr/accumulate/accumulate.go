// Code generated by fluent generator. DO NOT EDIT.

// Package accumulate defines the [Accumulate] type and its predefined values.
//
// Controls whether successive repeats of an SVG animation build on earlier repeats.
// With none, each repeat uses the same values. With sum, the animation accumulates
// its effect across repeats. This controls repetition, while additive controls
// how the animation combines with the underlying attribute value.
package accumulate

import (
	"github.com/jpl-au/fluent/node"
)

// Accumulate is a typed value for the SVG accumulate attribute.
//
// Controls whether successive repeats of an SVG animation build on earlier repeats.
// With none, each repeat uses the same values. With sum, the animation accumulates
// its effect across repeats. This controls repetition, while additive controls
// how the animation combines with the underlying attribute value.
type Accumulate []byte

// Variables for Accumulate values
var (
	// None Every repeat starts from the same place. This is the default.
	None = Accumulate("none")

	// Sum Each repeat starts where the last ended, so a repeating move travels further each time instead of resetting.
	Sum = Accumulate("sum")
)

// Custom allows setting a custom Accumulate value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) Accumulate {
	return Accumulate(node.EscapeAttribute(value))
}
