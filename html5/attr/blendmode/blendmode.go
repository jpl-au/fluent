// Code generated by fluent generator. DO NOT EDIT.

// Package blendmode defines the [BlendMode] type and its predefined values.
//
// Selects how feBlend combines the colours of its two filter inputs. The in
// attribute supplies the source and in2 supplies the backdrop. Modes such as
// multiply, screen and overlay use the same blending formulas as the CSS
// mix-blend-mode property.
package blendmode

import (
	"github.com/jpl-au/fluent/node"
)

// BlendMode is a typed value for the SVG feBlend mode attribute.
//
// Selects how feBlend combines the colours of its two filter inputs. The in
// attribute supplies the source and in2 supplies the backdrop. Modes such as
// multiply, screen and overlay use the same blending formulas as the CSS
// mix-blend-mode property.
type BlendMode []byte

// Variables for BlendMode values
var (
	// Normal The top input replaces the bottom one wherever it is opaque. This is the default.
	Normal = BlendMode("normal")

	// Multiply The two inputs are multiplied, which darkens: white leaves the other unchanged and black stays black.
	Multiply = BlendMode("multiply")

	// Screen The inverse of multiply, which lightens: black leaves the other unchanged and white stays white.
	Screen = BlendMode("screen")

	// Overlay Multiply where the bottom input is dark and screen where it is light, which raises contrast.
	Overlay = BlendMode("overlay")

	// Darken The darker of the two inputs is kept at each pixel.
	Darken = BlendMode("darken")

	// Lighten The lighter of the two inputs is kept at each pixel.
	Lighten = BlendMode("lighten")

	// ColorDodge The bottom input is brightened to reflect the top one.
	ColorDodge = BlendMode("color-dodge")

	// ColorBurn The bottom input is darkened to reflect the top one.
	ColorBurn = BlendMode("color-burn")

	// HardLight Overlay with the inputs swapped, which reads as a harsh spotlight.
	HardLight = BlendMode("hard-light")

	// SoftLight A gentler hard-light, as though a diffused light were shone on the bottom input.
	SoftLight = BlendMode("soft-light")

	// Difference The absolute difference between the inputs, so identical pixels become black.
	Difference = BlendMode("difference")

	// Exclusion Similar to difference but lower in contrast.
	Exclusion = BlendMode("exclusion")

	// Hue The hue of the top input with the saturation and luminosity of the bottom one.
	Hue = BlendMode("hue")

	// Saturation The saturation of the top input with the hue and luminosity of the bottom one.
	Saturation = BlendMode("saturation")

	// Color The hue and saturation of the top input with the luminosity of the bottom one, which tints without changing shading.
	Color = BlendMode("color")

	// Luminosity The luminosity of the top input with the hue and saturation of the bottom one.
	Luminosity = BlendMode("luminosity")
)

// Custom allows setting a custom BlendMode value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) BlendMode {
	return BlendMode(node.EscapeAttribute(value))
}
