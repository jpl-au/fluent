// Code generated by fluent generator. DO NOT EDIT.

// Package dominantbaseline defines the [DominantBaseline] type and its predefined values.
//
// Which baseline of the text is placed at the element's y position. Pair it
// with text-anchor to centre text on a point. A typed value so only valid
// dominant-baseline keywords compile.
package dominantbaseline

import (
	"github.com/jpl-au/fluent/node"
)

// DominantBaseline is a typed value for the SVG dominant-baseline attribute.
//
// Which baseline of the text is placed at the element's y position. Pair it
// with text-anchor to centre text on a point. A typed value so only valid
// dominant-baseline keywords compile.
type DominantBaseline []byte

// Variables for DominantBaseline values
var (
	// Auto The baseline follows the writing mode: alphabetic for horizontal text, central for vertical text. This is the SVG default.
	Auto = DominantBaseline("auto")

	// TextBottom The bottom of the em box is placed at y, so the text sits above the point including its descenders.
	TextBottom = DominantBaseline("text-bottom")

	// Alphabetic The alphabetic baseline is placed at y. Descenders such as the tail of a "g" hang below the point.
	Alphabetic = DominantBaseline("alphabetic")

	// Ideographic The ideographic baseline is placed at y, which is the bottom edge used by CJK glyphs.
	Ideographic = DominantBaseline("ideographic")

	// Middle The middle of the x-height is placed at y. This centres lower-case text on the point.
	Middle = DominantBaseline("middle")

	// Central The centre of the em box is placed at y. This centres text on the point regardless of case, and is the usual choice for labels centred on a marker.
	Central = DominantBaseline("central")

	// Mathematical The mathematical baseline is placed at y, which is where operators such as "+" and "=" centre.
	Mathematical = DominantBaseline("mathematical")

	// Hanging The hanging baseline is placed at y, the top edge used by Devanagari and similar scripts. In Latin text it puts the top of the glyphs at the point.
	Hanging = DominantBaseline("hanging")

	// TextTop The top of the em box is placed at y, so the text hangs below the point.
	TextTop = DominantBaseline("text-top")
)

// Custom allows setting a custom DominantBaseline value for edge cases or future specifications.
// Use this when the predefined constants don't cover your specific use case.
func Custom(value string) DominantBaseline {
	return DominantBaseline(node.EscapeAttribute(value))
}
